# Run 3 — opening tree review

## Integrated scope
Opening-only `opening_trees.gd` replaces the three orb canopies and square rendered trunks for the existing ten eligible trees. The original `.65 × height × .65` trunk colliders and original building-exclusion test remain. The garden's accepted geometry utilities are reused without editing its layout, foliage, solver, construction, crops, gate or source. No character, quest, save, camera or touch-input code changed.

Original project-authored opaque geometry; no asset purchase, downloaded model or paid generation. Forked branches and folded individual leaves are real-time meshes, not billboards or concept art. Deterministic seed per tree; two draw surfaces per tree; whole-opening leaf count bounded at 20,000 by test. This is an engineering bound, NOT measured iPad performance.

## Evidence and comparison
- `before/`: actual source baseline captured before presentation edits.
- `after/`: final frozen-source runs, original physical opening rescue/EN-RU route plus additive foliage, preserved cottage clearance, terrain/timber and garden checks.
- `pilot/`: superseded intermediate captures. Review found visible trunk section gaps and overly flat crown tiers. Final uses common section rings and staggered crown heights; only `after/` is current evidence.
- `red-corrected/`: meaningful baseline rejection, before implementation. Earlier `red/` includes a subsequently corrected harness issue (float equality and Godot auto-renamed nodes); do not use it as the behavior proof.

Bridge/grove captures use the real player camera at fixed seeded positions, same orientation, viewport, unrepaired stage, and English UI. They are composition fixtures, NOT a continuous route, child comprehension or hardware test. Physical input-route evidence is recorded separately.

## Visual inspection
Inspected final bridge and grove in both landscape and portrait. Branches, leaf edges and broken-up shadows replace smooth spheres; continuous trunk rings remove the visible pilot seams. Staggered tiers read less like flat umbrellas. Player/bridge remain readable and accepted scale unchanged.

**Still below the reference floor:** giant rounded perimeter hills, spherical outcrop, bare broad lawn, near-vertical creek lip, sparse planted transitions and placeholder-like collectible presentation. Some crowns sit behind the large unchanged objective HUD. Tree bark is color-varied geometry, not a newly textured production asset. Tree improvement is NOT whole-opening visual acceptance. Preserve these remaining defects in the next-card backlog rather than adding another destination and calling the opening finished.

## Browser result — not a full-suite pass
The real touch campaign completed rescue, Russian completion and replay (47 recorded checks), then exited 1 waiting for the outer page reload `load` event. The captured new intro and telemetry showed the saved reward, but the original runner never reached its remaining assertions. Its failure and screenshots remain under `browser/`.

The reload harness now matches initial navigation: DOM-content readiness followed by the existing engine-ready condition. No gameplay assertions were removed or relaxed. An independent fresh-context follow-up passed 16 touch/reload checks with current portrait/landscape captures. That test does not claim an earned-reward persistence route. The strict `check_run3_evidence.py` therefore still fails on missing full-run evidence; rerun the corrected complete campaign before further expansion.

## Acceptance boundaries
Native tests and exported-browser touch checks establish only their recorded functional scope. No physical iPad/Xcode test, owner visual approval, frame-rate measurement, or child-enjoyment playtest. Claude owns device acceptance. No new Scripture was authored; existing exact-text and bilingual guards remain required.
