# Irrigation geometry handoff (bounded, not visual acceptance)

Replaced scene-local inherited trim with solver-footprint channel lining. Five rendered footprints preserve solver areas, descend at the actual bed heights and share intake/crossing/furrow/catchment edges. Raised bank walls contain surfaces through the solver rim; main end closure directs the elbow west. The existing catchment now continues west to a free-outfall grate instead of a disconnected/uphill-looking tail. Solver unchanged. Gate boards move inside fixed guides at actual connection edges. Adjustable stop-board top matches 0.65 / 2 m crest. Waterfall faces depend on upstream/downstream heads and relevant opening. Support, channel floor, seal retain finite construction authority. All crops/rigs retained; the small front planting segment moved to clear the channel.

Visual iteration: inspected original `/tmp/jd-irrigation-flow-en.png`, then ordinary landscape/portrait captures. Reduced cyan, added separate coping joints and warm stone variation; removed disconnected trim. This is still procedural slab/box construction, NOT the requested better-than-Minecraft standard. Bespoke beveled/textured stonework, timber joinery/spindle detail, softer terrain transitions and fuller object-led demonstration remain asset/art work, not test-passing claims.

Real execution: original construction test `CONSTRUCTION_FAILURES=0`; unchanged host fixture walkthrough `CONSTRUCTION_HOST_FAILURES=0`; new inherited-assertion route test passed headless and xvfb fixed-fps60 with isolated XDG directories. `/tmp/jd-geometry-route2.log` native and `/tmp/jd-geometry-final.log` final headless both finish `CONSTRUCTION_HOST_FAILURES=0`. Initial route found Mira collision; revised approach walks around her west side, no player teleport for station travel. Prerequisite cave completion remains fixture setup; simulation is fast-forwarded through real campaign tick. No physical-device/browser claim.

Ordinary player Camera3D captures: `/tmp/jd-geometry-{ponding,flow-en,flow-ru}-{1280,720}.png`. Native captures precede the final addition of main end-wall and narrow catchment cheek walls; final headless rerun includes these. Capture route walks to observation position and changes normal camera yaw/pitch, no overview camera used for the saved images.

Incomplete: leak/overflow catchment link has no visible routed bypass. Current gameplay prohibits opening inlet before sealing, so baseline leak is unreachable; this does not justify claiming visible leak correctness. Grate is an outfall boundary marker, not a modeled downstream stream. Channel dressing remains non-colliding; test proves existing world navigation, not solid channel-bank traversal. Demo trays remain a minimal inherited presentation, not a full hands-on craftsmanship demonstration. Grounded EN/RU mechanism text added but visual/educational acceptance remains open. No structural simulation, crop response or agronomic validity claimed. Existing source dimensions/coefficients are illustrative, not construction advice.
